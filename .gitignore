# OS
.DS_Store
Thumbs.db
.idea/
*.swp

# Local secrets (each app has its own .env)
deploy.env

# Legacy artifacts
chms-deploy/
chms-deploy.tar.gz
hostinger-api/

# Optional infra secrets
docker/.env

# Flutter (mobile has its own .gitignore patterns)
mobile/build/
mobile/.dart_tool/
