# Hostinger / CI — pnpm v11 defaults strict-dep-builds=true which fails install
# when optional native builds (sharp, etc.) are skipped. Workspace yaml is primary;
# this is a fallback if Hostinger doesn't load pnpm-workspace.yaml.
strict-dep-builds=false
