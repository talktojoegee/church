# Hostinger runs default `pnpm install` (no custom install script).
# pnpm v11 fails on skipped native builds unless these are set:
strict-dep-builds=false
ignore-scripts=true
verify-deps-before-run=false
