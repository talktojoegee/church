packages:
  - "."

# pnpm v11: strictDepBuilds defaults true — must approve native addons or install fails.
# Scripts are skipped on Hostinger via --ignore-scripts (noexec .builds/).
strictDepBuilds: false
verifyDepsBeforeRun: false

allowBuilds:
  esbuild: false
  sharp: true
  unrs-resolver: true
