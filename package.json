{
  "name": "church-management-system",
  "version": "0.1.0",
  "private": true,
  "description": "Full-featured Church Management System (ChMS) built with NestJS + Next.js + MySQL",
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:prod": "bash scripts/build-prod.sh",
    "build:hostinger:api": "bash scripts/build-hostinger-api.sh",
    "build:hostinger:web": "bash scripts/build-hostinger-web.sh",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "start": "turbo run start",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:seed": "tsx prisma/seed.ts",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:prod": "docker compose -f docker/docker-compose.prod.yml --env-file docker/.env up -d --build",
    "docker:prod:down": "docker compose -f docker/docker-compose.prod.yml down",
    "docker:aws": "docker compose -f docker/docker-compose.aws.yml --env-file docker/.env up -d --build",
    "docker:aws:down": "docker compose -f docker/docker-compose.aws.yml down",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "mobile:pub": "cd apps/mobile && flutter pub get",
    "mobile:run": "cd apps/mobile && flutter run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "prettier": "^3.4.2",
    "prisma": "^6.1.0",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@chms/shared": "workspace:*",
    "@prisma/client": "^6.1.0",
    "argon2": "^0.41.1"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
