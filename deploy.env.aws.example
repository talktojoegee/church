# Copy to deploy.env for AWS EC2 deploys (deploy.env is gitignored).
#
# Your EC2 (paggglobal):
#   Elastic IP:  32.196.184.5
#   Region:      us-east-1
#   Key pair:    paggglobal_instance.pem
#   Instance:    t3.small (i-0fad3ffdcedff5407)

DEPLOY_HOST=32.196.184.5
DEPLOY_USER=ubuntu
DEPLOY_PATH=/opt/chms
DEPLOY_SSH_KEY=~/.ssh/paggglobal_instance.pem
# DEPLOY_SSH_PORT=22

GIT_REPO=https://github.com/talktojoegee/church.git
GIT_BRANCH=main

# Health check after deploy
HEALTH_URL=https://api.paggglobal.org/api/health

# Baked into web Docker image at build time (also set in docker/.env on server)
NEXT_PUBLIC_API_URL=https://api.paggglobal.org/api
NEXT_PUBLIC_APP_NAME="Power And Glory Generation"
